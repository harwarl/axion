use colored::Colorize;

use crate::error::Result;

pub fn add(name: &String, kind: &String) -> Result<()> {
    // check if there is a cargo.toml i.e it's a cargo project throw if it isn't
    // create a prompt to add files
    // Generate files and directories needed
    Ok(())
}
